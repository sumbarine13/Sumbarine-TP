--// Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

--// Player
local lp = Players.LocalPlayer

--// GUI
local gui = Instance.new("ScreenGui")
gui.Name = "AstrexHub"
gui.ResetOnSpawn = false
gui.Parent = lp:WaitForChild("PlayerGui")

local main = Instance.new("Frame")
main.Size = UDim2.new(0, 340, 0, 260)
main.Position = UDim2.new(0.5, -170, 0.4, -130)
main.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
main.BorderSizePixel = 0
main.Active = true
main.Draggable = true
main.Parent = gui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 9)
corner.Parent = main

local stroke = Instance.new("UIStroke")
stroke.Thickness = 3
stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
stroke.Color = Color3.fromRGB(255, 0, 0)
stroke.Parent = main

--// Discord Button
local discordBtn = Instance.new("TextButton")
discordBtn.Size = UDim2.new(0, 145, 0, 28)
discordBtn.Position = UDim2.new(0, 10, 0, 10)
discordBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
discordBtn.Text = "Join Discord"
discordBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
discordBtn.TextSize = 11
discordBtn.Font = Enum.Font.GothamMedium
discordBtn.TextXAlignment = Enum.TextXAlignment.Right
discordBtn.Parent = main

local dPadding = Instance.new("UIPadding")
dPadding.PaddingRight = UDim.new(0, 10)
dPadding.Parent = discordBtn

local dCorner = Instance.new("UICorner")
dCorner.CornerRadius = UDim.new(0, 6)
dCorner.Parent = discordBtn

local dIcon = Instance.new("ImageLabel")
dIcon.Size = UDim2.new(0, 18, 0, 18)
dIcon.Position = UDim2.new(0, 6, 0.5, -9)
dIcon.BackgroundTransparency = 1
dIcon.Image = "rbxassetid://11419713430"
dIcon.Parent = discordBtn

discordBtn.MouseButton1Click:Connect(function()
	if setclipboard then
		setclipboard("https://discord.gg/CTjeFG5bGt")
		discordBtn.Text = "Link Copied!"
		task.wait(2)
		discordBtn.Text = "Join Discord"
	end
end)

--// Close Button
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 28, 0, 28)
closeBtn.Position = UDim2.new(1, -38, 0, 10)
closeBtn.BackgroundColor3 = Color3.fromRGB(30, 15, 15)
closeBtn.Text = "X"
closeBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
closeBtn.TextSize = 14
closeBtn.Font = Enum.Font.GothamBold
closeBtn.Parent = main

local cCorner = Instance.new("UICorner")
cCorner.CornerRadius = UDim.new(0, 6)
cCorner.Parent = closeBtn

closeBtn.MouseButton1Click:Connect(function()
	gui:Destroy()
end)

--// Title
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 45)
title.Position = UDim2.new(0, 0, 0, 25)
title.BackgroundTransparency = 1
title.Text = "Astrex Hub"
title.TextSize = 28
title.Font = Enum.Font.GothamBold
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Parent = main

local warning = Instance.new("TextLabel")
warning.Size = UDim2.new(1, 0, 0, 20)
warning.Position = UDim2.new(0, 0, 0, 62)
warning.BackgroundTransparency = 1
warning.Text = "(test gui doesnt work)"
warning.TextColor3 = Color3.fromRGB(120, 120, 120)
warning.TextSize = 13
warning.Font = Enum.Font.GothamMedium
warning.Parent = main

--// Buttons
local container = Instance.new("Frame")
container.Size = UDim2.new(1, -40, 1, -135)
container.Position = UDim2.new(0, 20, 0, 105)
container.BackgroundTransparency = 1
container.Parent = main

local layout = Instance.new("UIListLayout")
layout.Padding = UDim.new(0, 8)
layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
layout.Parent = container

local function makeBtn(text)
	local b = Instance.new("TextButton")
	b.Size = UDim2.new(1, 0, 0, 34)
	b.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
	b.Text = text
	b.TextColor3 = Color3.fromRGB(255, 255, 255)
	b.Font = Enum.Font.GothamSemibold
	b.TextSize = 14
	b.AutoButtonColor = false
	b.Parent = container

	local bc = Instance.new("UICorner")
	bc.CornerRadius = UDim.new(0, 5)
	bc.Parent = b
end

makeBtn("Save Pos")
makeBtn("TP to Pos")
makeBtn("Desync")

--// Footer
local footer = Instance.new("TextLabel")
footer.Size = UDim2.new(1, 0, 0, 20)
footer.Position = UDim2.new(0, 0, 1, -25)
footer.BackgroundTransparency = 1
footer.Text = "astrex hub ontop!"
footer.TextColor3 = Color3.fromRGB(80, 80, 80)
footer.TextSize = 11
footer.Font = Enum.Font.GothamItalic
footer.Parent = main

--// 🌈 Smooth Rainbow Effect (FIXED)
RunService.RenderStepped:Connect(function()
	local hue = (os.clock() * 0.15) % 1
	local color = Color3.fromHSV(hue, 1, 1)

	stroke.Color = color
	title.TextColor3 = color
end)
